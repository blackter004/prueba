cmd y powershel: el cmd es mas antiguo ymas limitadoy el powershell nos va mejor

con el comando help me muestra la lista de comandos

cd: nos permite viajar entre carpetas
dir: nos muestra la lista de los directorios a los que nos podemos posicionar
si tengo carpetas con espacios, las coloco en la terminal entre comillas

cls/clear: limpiar el cmd

si en la carpeta a la que quiero entrar escribo en la barra de navegacion powershell o cmd, me va a abrir la terminal automaticamente ya ubicada en esa carpeta (una buena forma de ubicarme en el archivo rapidamente)

MKDIR o MD: nos sirve para cerar una carpeta ">md carpeta-ejemplo"

move: para mover archivos ">move archivo-que-deseamos-mover carpeta-destino" ">move app.js carpeta-ejemplo" ">move index.html "C:\users\carpeta\carpeta2""

ctrl + c: para detener un proceso

ni: crar archivos en poweshell ">ni index.html"

terminal de vs: en las configuraciones podemos decidir la terminal por defecto (por lo general se usa poweshell)

trabajar como administrador cuando haga instalaciones en el sistema con cmd

github: user: blackter004 - password: falcao210

instalar apps en linux: sudo snap install app

comandos git:
git version: aparece la version de git

git help: lista de comandos

git init: es para inicar un repositorio (le estoy diciendo que todo mi proyecto se va a convertir en un repositorio)

git status -s: escanea lo que ha pasado

git add .: agrega todo al repositorio

git commit -m "hice tal cosa": etiqueta todo en lo que hice cambios en mi repositorio

si el archivo dice "M": nos esta diciendo que lo hemos modificado

git log --oneline: nos muestra la lista de commits que hemos hecho

iniciar sesion: >git config --global user.name "blackter004" despues: >git config --global user.email "sebas.naranjocastano@gmail.com" 

git remote add origin link-repositorio: para llamar a nuestro repositorio/descargarlo en mi dispositivo

git branch -M master: para ubicarse en la rama main (principal)





